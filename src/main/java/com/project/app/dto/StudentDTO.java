package com.project.app.dto;

public class StudentDTO extends HumanDTO {

    public StudentDTO(Long id, int age, String sex, double salary, String type) {
        super(id, age, sex, salary, type);
    }
}
